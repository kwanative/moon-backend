class Moon {
    constructor(id, moonBalance, latestPrice) {
            this.id = id;
            this.moonBalance = moonBalance;
            this.latestPrice = latestPrice;
    }
}

module.exports = Moon;